#include <stdio.h>

/**
 * main - Program that prints infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinite loop incoming :(\n");

	i = 0;

	/**
	 * while (i < 10)
	 * {
	 *   putchar(i);
	 * }
	 */
	printf("Infinite loop avoide! \\o/\n");
	return (0);
}

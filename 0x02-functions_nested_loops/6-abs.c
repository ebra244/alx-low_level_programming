#include "main.h"
/**
 * _abs - Function that computes the absolute value of an integer
 * @n: an integer numer
 * Return: Always 0 or positive (success)
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

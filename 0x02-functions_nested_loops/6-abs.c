#include "main.h"
/**
 *_abs - Function that computes absolute value 
 * @n: Argument to be passed
 * Return: the absolute value of int
 */
int _abs(int n)
{

	if (n < 0)
		return (-n);
	else
		return (n);
}

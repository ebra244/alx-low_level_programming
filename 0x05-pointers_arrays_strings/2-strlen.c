#include "main.h"
#include <string.h>
/**
 * _strlen - Function that returns the length of a string
 * @s: String to be checked
 * Return: 1 (On Success)
 */
int _strlen(char *s)
{
	return (strlen(s));
}

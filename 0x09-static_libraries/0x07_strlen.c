#include "main.h"
#include <string.h>
/**
 * _strlen - Function that returns the length of a string
 * @s: String whose length will be returned
 * Return: 1 On success
 */
int _strlen(char *s)
{
	return (strlen(s));
}
